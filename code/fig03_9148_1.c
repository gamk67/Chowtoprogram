   while ( grade != -1 ) {
      total = total + grade;
      counter = counter + 1;      
      /* get next grade from user */
      printf( "Enter grade, -1 to end: " );
      scanf("%d", &grade);
   } /* end while */
